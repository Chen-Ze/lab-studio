import { sharedDataRecipeRecipeOutput } from './shared-data-recipe-recipe-output';

describe('sharedDataRecipeRecipeOutput', () => {
  it('should work', () => {
    expect(sharedDataRecipeRecipeOutput()).toEqual(
      'shared-data-recipe-recipe-output'
    );
  });
});

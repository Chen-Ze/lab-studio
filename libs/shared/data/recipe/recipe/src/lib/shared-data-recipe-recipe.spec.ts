import { sharedDataRecipeRecipe } from './shared-data-recipe-recipe';

describe('sharedDataRecipeRecipe', () => {
  it('should work', () => {
    expect(sharedDataRecipeRecipe()).toEqual('shared-data-recipe-recipe');
  });
});

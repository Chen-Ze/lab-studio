import { sharedExperimentsCalculatorRecipe } from './shared-experiments-calculator-recipe';

describe('sharedExperimentsCalculatorRecipe', () => {
  it('should work', () => {
    expect(sharedExperimentsCalculatorRecipe()).toEqual(
      'shared-experiments-calculator-recipe'
    );
  });
});

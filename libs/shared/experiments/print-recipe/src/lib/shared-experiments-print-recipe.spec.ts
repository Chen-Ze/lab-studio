import { sharedExperimentsPrintRecipe } from './shared-experiments-print-recipe';

describe('sharedExperimentsPrintRecipe', () => {
  it('should work', () => {
    expect(sharedExperimentsPrintRecipe()).toEqual(
      'shared-experiments-print-recipe'
    );
  });
});

import { frontFeatureExperimentTabExperimentEnvironmentReducerProvider } from './front-feature-experiment-tab-experiment-environment-reducer-provider';

describe('frontFeatureExperimentTabExperimentEnvironmentReducerProvider', () => {
  it('should work', () => {
    expect(
      frontFeatureExperimentTabExperimentEnvironmentReducerProvider()
    ).toEqual(
      'front-feature-experiment-tab-experiment-environment-reducer-provider'
    );
  });
});

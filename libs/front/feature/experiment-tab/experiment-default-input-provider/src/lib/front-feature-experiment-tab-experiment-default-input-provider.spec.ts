import { frontFeatureExperimentTabExperimentDefaultInputProvider } from './front-feature-experiment-tab-experiment-default-input-provider';

describe('frontFeatureExperimentTabExperimentDefaultInputProvider', () => {
  it('should work', () => {
    expect(frontFeatureExperimentTabExperimentDefaultInputProvider()).toEqual(
      'front-feature-experiment-tab-experiment-default-input-provider'
    );
  });
});

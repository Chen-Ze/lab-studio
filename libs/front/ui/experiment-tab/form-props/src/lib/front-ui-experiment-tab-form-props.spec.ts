import { frontUiExperimentTabFormProps } from './front-ui-experiment-tab-form-props';

describe('frontUiExperimentTabFormProps', () => {
  it('should work', () => {
    expect(frontUiExperimentTabFormProps()).toEqual(
      'front-ui-experiment-tab-form-props'
    );
  });
});

import { frontUiExperimentTabInputProps } from './front-ui-experiment-tab-input-props';

describe('frontUiExperimentTabInputProps', () => {
  it('should work', () => {
    expect(frontUiExperimentTabInputProps()).toEqual(
      'front-ui-experiment-tab-input-props'
    );
  });
});

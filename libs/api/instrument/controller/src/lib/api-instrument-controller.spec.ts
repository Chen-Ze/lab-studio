import { apiInstrumentController } from './api-instrument-controller';

describe('apiInstrumentController', () => {
  it('should work', () => {
    expect(apiInstrumentController()).toEqual('api-instrument-controller');
  });
});

import { apiExperimentExperimentWorker } from './api-experiment-experiment-worker';

describe('apiExperimentExperimentWorker', () => {
  it('should work', () => {
    expect(apiExperimentExperimentWorker()).toEqual(
      'api-experiment-experiment-worker'
    );
  });
});

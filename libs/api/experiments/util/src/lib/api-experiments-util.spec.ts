import { apiExperimentsUtil } from './api-experiments-util';

describe('apiExperimentsUtil', () => {
  it('should work', () => {
    expect(apiExperimentsUtil()).toEqual('api-experiments-util');
  });
});
